ResumeViewer.Models.Item = Backbone.Model.extend({
  defaults: {
    item_name: null,
    item_value: null
  },

  initialize: function() {
    //this.set('avatar', _.random(1, 15) + '.jpg');
  }
}); 